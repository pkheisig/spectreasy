% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build_reference_matrix.R
\name{build_reference_matrix}
\alias{build_reference_matrix}
\title{Build a Reference Matrix from Single-Color Controls}
\usage{
build_reference_matrix(
  input_folder = "scc",
  output_folder = "gating_and_spectrum_plots",
  save_qc_plots = FALSE,
  control_df = NULL,
  exclude_af = FALSE,
  af_n_bands = "auto",
  af_bands_per_file = NULL,
  af_max_cells = 50000,
  af_auto_max_bands = 100,
  af_min_cluster_events = 20,
  af_min_cluster_proportion = 0.005,
  af_n_bands_sensitivity = 1.5,
  af_refine = FALSE,
  af_refine_problem_quantile = 0.99,
  af_deduplicate = FALSE,
  af_deduplication_threshold = 0.99,
  af_contaminant_threshold = 0.99,
  seed = NULL,
  default_sample_type = "beads",
  cytometer = "auto",
  use_scatter_gating = TRUE,
  use_af_cosine_scc_selection = TRUE,
  clean_scc_with_unstained = TRUE,
  scc_background_method = c("scatter_knn", "none"),
  scc_background_k = 3L,
  histogram_pct_beads = 0.98,
  histogram_direction_beads = "right",
  histogram_pct_cells = 0.35,
  histogram_direction_cells = "right",
  outlier_percentile = 0.02,
  debris_percentile = 0.08,
  bead_gate_scale = 1.3,
  max_clusters = 10,
  min_cluster_proportion = 0.03,
  gate_contour_beads = 0.95,
  gate_contour_cells = 0.8,
  subsample_n = 5000
)
}
\arguments{
\item{input_folder}{Directory containing SCC `.fcs` files.}

\item{output_folder}{Directory where gating/spectrum plots are written.}

\item{save_qc_plots}{Logical; if `TRUE`, write FSC/SSC, intensity-gate,
optional spectral-selection, and spectrum plots. When `FALSE` (default),
the function returns the matrix without writing QC files.}

\item{control_df}{Optional control mapping as a data.frame or CSV path.
Expected columns: `filename`, `fluorophore`, and `channel`.}

\item{exclude_af}{Logical; if `TRUE`, ignore AF/unstained controls entirely,
even when they are present in `control_df` or the SCC folder.}

\item{af_n_bands}{Number of SOM nodes used to extract AF basis signatures
from pooled unstained/AF control events, or `"auto"` to use
`af_auto_max_bands`. A mean AF row is prepended to the SOM nodes, so the
default creates 100 SOM rows plus the mean row before QC/removal steps.}

\item{af_bands_per_file}{Deprecated compatibility argument. Multiple AF
sources are pooled before SOM extraction; `af_n_bands`/`af_auto_max_bands`
control the size of the one shared AF bank.}

\item{af_max_cells}{Maximum number of scatter-gated AF events used when
deriving AF basis signatures.}

\item{af_auto_max_bands}{Maximum SOM nodes that `"auto"` may create before
prepending the mean AF row. Default is 100.}

\item{af_min_cluster_events}{Compatibility argument retained for older
workflows.}

\item{af_min_cluster_proportion}{Compatibility argument retained for older
workflows.}

\item{af_n_bands_sensitivity}{Compatibility argument retained for older
workflows.}

\item{af_refine}{Logical; if `TRUE`, run a second AF-library refinement pass
that modulates SOM AF spectra from high-error unstained cells.}

\item{af_refine_problem_quantile}{Quantile used to choose high-error
unstained cells for AF refinement. Default is 0.99.}

\item{af_deduplicate}{Logical; if `TRUE`, remove near-identical AF spectra
after SOM extraction/refinement using cosine similarity. Default is
`FALSE` for backward compatibility.}

\item{af_deduplication_threshold}{Cosine similarity threshold used when
`af_deduplicate = TRUE`. Default is 0.99.}

\item{af_contaminant_threshold}{Cosine similarity threshold used to remove
AF candidates that are too similar to known fluorophore spectra. Default is
0.99.}

\item{seed}{Optional integer seed for deterministic subsampling/clustering.}

\item{default_sample_type}{Fallback type when filename heuristics are ambiguous (`"beads"` or `"cells"`).}

\item{cytometer}{Cytometer name used as a channel-mapping hint. The default,
`"auto"`, infers the cytometer from FCS detector names when possible.}

\item{use_scatter_gating}{Logical; if `TRUE` (default), keep broad FSC/SSC
cleanup and use the intensity-vs-FSC GMM/EM selector for SCC events. If
`FALSE`, use the legacy one-dimensional histogram gate.}

\item{use_af_cosine_scc_selection}{Logical; if `TRUE` (default), use the
adaptive AF projection/cosine selector for cell SCCs when AF controls are
available. This mode scores events by low AF similarity, residual
target-channel brightness, and target-channel dominance, then keeps the
high-score component. Bead SCCs continue to use the GMM/EM
intensity-vs-FSC selector after broad FSC/SSC cleanup.}

\item{clean_scc_with_unstained}{Logical; if `TRUE`, cell SCC spectra are
cleaned with scatter-matched unstained/AF events before spectrum
derivation. Bead controls use a scatter-gated unstained bead control as
their negative spectrum when one is available; otherwise they keep the
positive/negative populations estimated from the stained bead control.
If `use_af_cosine_scc_selection = TRUE`, this cleanup is required and is
automatically enabled with a warning.}

\item{scc_background_method}{Background method for cell SCC cleaning.
`"scatter_knn"` matches each stained event to unstained events by FSC/SSC.
Use `"none"` to disable matched background subtraction.}

\item{scc_background_k}{Number of nearest unstained events averaged for
`"scatter_knn"` cell SCC background subtraction.}

\item{histogram_pct_beads}{Quantile width for the bead histogram gate.}

\item{histogram_direction_beads}{Histogram gate direction for beads: `"right"` starts at the median,
`"both"` centers on the median, and `"left"` ends at the median.}

\item{histogram_pct_cells}{Quantile width for the default cell histogram
gate. When `use_af_cosine_scc_selection = TRUE`, this acts as a
conservative maximum cap for the experimental adaptive AF-score selector
rather than a fixed fraction to keep.}

\item{histogram_direction_cells}{Histogram gate direction for cells: `"right"` starts at the median,
`"both"` centers on the median, and `"left"` ends at the median.}

\item{outlier_percentile}{Upper-tail FSC/SSC filtering percentile.}

\item{debris_percentile}{Lower FSC range fraction for cell/unstained controls.
For example, `0.08` excludes events below 8\% of the per-file high FSC scale.}

\item{bead_gate_scale}{Ellipse scaling factor for bead FSC/SSC gate.}

\item{max_clusters}{Maximum number of GMM components tested.}

\item{min_cluster_proportion}{Minimum population proportion kept from GMM fit.}

\item{gate_contour_beads}{Contour probability for bead gating ellipse/hull.}

\item{gate_contour_cells}{Contour probability for cell gating ellipse/hull.}

\item{subsample_n}{Maximum number of events used for GMM fitting per file.}
}
\value{
Numeric matrix with rows = fluorophores and columns = detectors
  (normalized spectra). The matrix carries SCC-derived detector noise floors
  in `attr(M, "detector_noise")` for default WLS unmixing.
}
\description{
Reads SCC FCS files, performs broad FSC/SSC cleanup, uses the default
GMM/EM intensity-vs-FSC selector for bead and cell controls, then computes
one normalized spectrum per fluorophore. The experimental AF-cosine cell
SCC selector is available only when explicitly enabled.
}
\details{
This is the core matrix-construction step used before unmixing.
}
\examples{
if (interactive()) {
  M <- build_reference_matrix(
    input_folder = "scc",
    output_folder = "spectreasy_outputs/build_reference_plots",
    save_qc_plots = TRUE,
    control_df = "fcs_mapping.csv",
    cytometer = "auto"
  )

  M_fast <- build_reference_matrix(
    input_folder = "scc",
    save_qc_plots = FALSE
  )
}
}
