% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gate_controls.R
\name{gate_controls}
\alias{gate_controls}
\title{Gate Single-Color Controls Interactively}
\usage{
gate_controls(
  scc_dir = "scc",
  control_file = "fcs_mapping.csv",
  gate_file = "ssc_gate_config.csv",
  port = 8000,
  open_browser = TRUE,
  dev_mode = FALSE,
  dev_frontend_port = NULL
)
}
\arguments{
\item{scc_dir}{Directory containing SCC `.fcs` files. Defaults to `"scc"`.}

\item{control_file}{Control mapping CSV. Defaults to `"fcs_mapping.csv"`.}

\item{gate_file}{Output gate CSV. Relative paths are resolved from the current
working directory. Defaults to `"ssc_gate_config.csv"`.}

\item{port}{API port. Defaults to `8000`.}

\item{open_browser}{Logical. Open browser automatically?}

\item{dev_mode}{Logical. Use the Vite dev server instead of bundled assets.}

\item{dev_frontend_port}{Optional Vite frontend port used only with
`dev_mode = TRUE`. When `NULL`, the first free port from 5174 onward is used.}
}
\value{
Invisibly returns the normalized gate CSV path. The function blocks
  while the GUI is running.
}
\description{
Launches the browser-based manual gating interface for single-color controls.
The gate CSV is written to the current working directory by default so it can
be reused by [unmix_controls()] or inspected directly.
}
\examples{
if (interactive()) {
  gate_controls(scc_dir = "scc", open_browser = FALSE)
}
}
