% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_functions.R
\name{plot_unmixing_scatter_matrix}
\alias{plot_unmixing_scatter_matrix}
\title{Plot NxN Unmixing Scatter Matrix}
\usage{
plot_unmixing_scatter_matrix(
  unmixed_list,
  sample_to_marker = NULL,
  markers = NULL,
  marker_display = NULL,
  output_file = "unmixing_scatter_matrix.png",
  max_points_per_sample = 3000,
  transform = c("none", "asinh"),
  asinh_cofactor = 150,
  panel_size_mm = 30
)
}
\arguments{
\item{unmixed_list}{Output list from `unmix_samples()`.}

\item{sample_to_marker}{Named character vector mapping sample IDs (basename
without extension) to fluorophore/marker names.}

\item{markers}{Optional character vector of marker columns to include.}

\item{marker_display}{Optional named character vector mapping primary marker
names to display labels (for example `"BUV395 / CD45RA"`).}

\item{output_file}{Path to PNG output.}

\item{max_points_per_sample}{Maximum events sampled per sample for plotting.}

\item{transform}{One of `"asinh"` or `"none"`.}

\item{asinh_cofactor}{Cofactor used when `transform = "asinh"`.}

\item{panel_size_mm}{Size per matrix panel in millimeters.}
}
\value{
Invisibly returns `output_file`.
}
\description{
Creates a lower-triangle marker-vs-marker scatter matrix from unmixed SCC output.
Each populated panel uses events from only one single-stain file (row stain),
while the column stain is permuted across the lower triangle.
}
\examples{
\dontrun{
plot_unmixing_scatter_matrix(
  unmixed_list = unmixed_list,
  markers = rownames(M),
  output_file = "scc_unmixing_scatter_matrix.png"
)
}
}
