% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/adjust_matrix.R
\name{launch_gui}
\alias{launch_gui}
\title{Launch spectreasy Interactive Adjustment GUI}
\usage{
launch_gui(
  matrix_dir = NULL,
  samples_dir = NULL,
  port = 8000,
  open_browser = TRUE,
  dev_mode = FALSE
)
}
\arguments{
\item{matrix_dir}{Directory containing matrix CSV files. If `NULL`, defaults to
`spectreasy_outputs/unmix_controls` when it exists under the current working
directory, otherwise the current working directory.}

\item{samples_dir}{Directory containing FCS sample files. If `NULL`, defaults to
the `samples` folder under the current working directory or inferred project
directory when it exists, then falls back to
`spectreasy_outputs/unmix_samples/unmixed_fcs` or
`spectreasy_outputs/unmix_samples` when available. Otherwise, uses the
`samples` subfolder of `matrix_dir`.}

\item{port}{API port (default: 8000)}

\item{open_browser}{Logical. Open browser automatically? (default: TRUE)}

\item{dev_mode}{Logical. If `FALSE` (default), serves bundled GUI assets from the
installed package and requires no Node.js/npm on user machines. If `TRUE`, starts
the Vite dev server (`npm run dev`) from the packaged GUI source folder.}
}
\value{
Invisibly returns NULL. This function blocks while the API is running.
}
\description{
`launch_gui()` is kept as a backward-compatible alias for [adjust_matrix()].
}
