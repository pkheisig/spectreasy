% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plot_functions.R
\name{plot_spectra}
\alias{plot_spectra}
\title{Plot Spectral Overlays}
\usage{
plot_spectra(
  ref_matrix,
  pd = NULL,
  output_file = NULL,
  width = 250,
  height = 100,
  unit = "mm",
  dpi = 600,
  theme_custom = NULL,
  annotate_peaks = "auto",
  peak_label_max = 40L,
  peak_label_size = 2.6
)
}
\arguments{
\item{ref_matrix}{Reference matrix (Markers x Detectors)}

\item{pd}{Optional pData for descriptive labels}

\item{output_file}{Optional path to save the plot. Set `NULL` to return the plot without writing a file.}

\item{width}{Plot width.}

\item{height}{Plot height.}

\item{unit}{Plot size unit.}

\item{dpi}{Output resolution.}

\item{theme_custom}{Optional ggplot theme object (reserved).}

\item{annotate_peaks}{`"auto"` (default), `TRUE`, or `FALSE`. In auto mode,
compact peak labels are added for regular marker spectra but skipped for
all-AF-bank overlays.}

\item{peak_label_max}{Maximum number of spectra to label in auto mode.}

\item{peak_label_size}{Text size for peak labels.}
}
\value{
A `ggplot` object.
}
\description{
Plot Spectral Overlays
}
\examples{
m <- diag(3)
rownames(m) <- c("FITC", "PE", "APC")
colnames(m) <- c("B2-A", "YG1-A", "R1-A")
p <- plot_spectra(ref_matrix = m, output_file = NULL)
print(p)
}
